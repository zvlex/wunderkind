<% item = t('products.show.items') %>
<% vertical_bar = t('products.show.symbol') %>
<% cart_id = cart_path(cart.id) %>

<div><%= link_to t('products.show.your_cart'), cart_id %></div>
<div class="total_cart">
  <% if get_loc == 'en' %>
    <span><%= link_to pluralize(@cart.items_quantity, item), cart_id %></span><%= vertical_bar %>
  <% else %>
      <span><%= link_to "#{@cart.items_quantity} #{item}", cart_id %></span><%= vertical_bar %>
  <% end %>
  <span><%= link_to "#{@cart.total_price} #{t('products.show.currency')}", cart_id %></span>
</div>



